import { StyleSheet } from "react-native";
import { Colors } from "../../../app.constants";

const parametersScreenStyles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: Colors.background,
    },
});

export default parametersScreenStyles;